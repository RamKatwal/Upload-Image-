/** Providhy company logo slot size (square). */
// Export bigger than the UI slot so it stays crisp when displayed.
// In real backend integration you can still downscale to 96×96 if needed.
export const LOGO_OUTPUT_SIZE = 512;
